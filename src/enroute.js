import { Navigate } from "react-router";

const notification = 'You must be an admin to add car';

const enroute = () => {
  alert(notification);
  <p>Sorry we are not able to redirect you to root</p>
};

export default enroute;
